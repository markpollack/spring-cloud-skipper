spring:
  cloud:
    skipper:
      server:
        synchonizeIndexOnContextRefresh: true
        packageRepositories:
         -
           name: test
           url: "classpath:/repositories/binaries/test"
        platform:
          local:
            accounts:
              test:
                shutdownTimeout: 60
                javaOpts: "-Dtest=foo"
              localDevDebug:
                javaOpts: "-Xdebug"